<h1><%= @my_project.name%></h1>
<ul>
	<% @my_entries.each do |entry| %>
	<li>
		
	<%= entry.hours %> h
	<%= entry.minutes%> m -
	<%= entry.date %>
	<p> <%= the_entry.comment %></p>
	</li>

	<% end -%>

</ul>

<a href="/projects/ <%= @my_project.id %> /time_entries/new">Add time entry to  <%= @my_project.name %></a>